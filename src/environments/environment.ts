export const environment = {
  production: false,
  apiBaseUrl: 'https://avis-unpersecutive-negatively.ngrok-free.dev/', // keep empty during local dev to use mocks
  // apiBaseUrl: '',
  useMocks: false,
} as const;
